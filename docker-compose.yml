services:
  emotion:
    build: .
    image: emotion:latest
    container_name: emotion
    restart: unless-stopped
    ports:
      - "8096:8096"
    # Mount your media root here. The path inside the container (/data) is what
    # you'll pass to POST /admin/library/scan as "root".
    volumes:
      - ./media:/data:ro
    environment:
      APP_NAME: emotion
      APP_LOG_LEVEL: info

      DB_DRIVER: mysql
      DB_HOST: mysql
      DB_PORT: 3306
      DB_DATABASE: emotion
      DB_USERNAME: emotion
      DB_PASSWORD: emotion_pwd

      VALKEY_HOST: valkey
      VALKEY_PORT: 6379

      # API_KEY is the admin key used by Sakura_embyboss and other tools.
      API_KEY: change-me-please

      # TMDB metadata (optional). Leave empty to disable.
      # Get a free v3 key at https://www.themoviedb.org/settings/api
      TMDB_API_KEY: ""
      TMDB_LANGUAGE: zh-CN
      TMDB_AUTO_SCRAPE: "true"

      EMBY_VERSION: 4.8.10.0
      EMBY_ID: emotion
    depends_on:
      mysql:
        condition: service_healthy

  mysql:
    image: mysql:8
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: emotion
      MYSQL_USER: emotion
      MYSQL_PASSWORD: emotion_pwd
      MYSQL_ROOT_PASSWORD: root_pwd
    command:
      - --character-set-server=utf8mb4
      - --collation-server=utf8mb4_unicode_ci
    volumes:
      - mysql-data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-p$$MYSQL_ROOT_PASSWORD"]
      interval: 5s
      timeout: 5s
      retries: 20

  valkey:
    image: valkey/valkey:7-alpine
    restart: unless-stopped
    command: ["valkey-server", "--save", "", "--appendonly", "no"]

volumes:
  mysql-data:
